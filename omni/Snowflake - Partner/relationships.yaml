- join_from_view: ecomm__events
  join_to_view: ecomm__users
  join_type: always_left
  on_sql: ${ecomm__events.user_id} = ${ecomm__users.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__int_ecomm__events
  join_to_view: ecomm__users
  join_type: always_left
  on_sql: ${ecomm__int_ecomm__events.user_id} = ${ecomm__users.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__int_ecomm__inventory_items
  join_to_view: ecomm__products
  join_type: always_left
  on_sql: ${ecomm__int_ecomm__inventory_items.product_id} = ${ecomm__products.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__int_ecomm__order_items
  join_to_view: ecomm__users
  join_type: always_left
  on_sql: ${ecomm__int_ecomm__order_items.user_id} = ${ecomm__users.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__int_ecomm__order_items
  join_to_view: ecomm__inventory_items
  join_type: always_left
  on_sql: ${ecomm__int_ecomm__order_items.inventory_item_id} =
    ${ecomm__inventory_items.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__inventory_items
  join_to_view: ecomm__products
  join_type: always_left
  on_sql: ${ecomm__inventory_items.product_id} = ${ecomm__products.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__order_items
  join_to_view: ecomm__users
  join_type: always_left
  on_sql: ${ecomm__order_items.user_id} = ${ecomm__users.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__order_items
  join_to_view: ecomm__inventory_items
  join_type: always_left
  on_sql: ${ecomm__order_items.inventory_item_id} = ${ecomm__inventory_items.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__order_items_agg
  join_to_view: ecomm__users
  join_type: always_left
  on_sql: ${ecomm__order_items_agg.user_id} = ${ecomm__users.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__products
  join_to_view: ecomm__distribution_centers
  join_type: always_left
  on_sql: ${ecomm__products.distribution_center_id} = ${ecomm__distribution_centers.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__sessions
  join_to_view: ecomm__users
  join_type: always_left
  on_sql: ${ecomm__sessions.user_id} = ${ecomm__users.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__stg_ecomm__events
  join_to_view: ecomm__users
  join_type: always_left
  on_sql: ${ecomm__stg_ecomm__events.user_id} = ${ecomm__users.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__stg_ecomm__inventory_items
  join_to_view: ecomm__products
  join_type: always_left
  on_sql: ${ecomm__stg_ecomm__inventory_items.product_id} = ${ecomm__products.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__stg_ecomm__order_items
  join_to_view: ecomm__users
  join_type: always_left
  on_sql: ${ecomm__stg_ecomm__order_items.user_id} = ${ecomm__users.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__stg_ecomm__order_items
  join_to_view: ecomm__inventory_items
  join_type: always_left
  on_sql: ${ecomm__stg_ecomm__order_items.inventory_item_id} =
    ${ecomm__inventory_items.id}
  relationship_type: assumed_many_to_one

- join_from_view: ecomm__stg_ecomm__products
  join_to_view: ecomm__distribution_centers
  join_type: always_left
  on_sql: ${ecomm__stg_ecomm__products.distribution_center_id} =
    ${ecomm__distribution_centers.id}
  relationship_type: assumed_many_to_one

# Relationship is defined in the 'orders' dbt semantic model (models/marts/orders.yml)
- join_from_view: dbt_sleon_prod__orders
  join_to_view: dbt_sleon_prod__locations
  join_type: always_left
  on_sql: ${dbt_sleon_prod__orders.location_id} =
    ${dbt_sleon_prod__locations.location_id}
  relationship_type: assumed_many_to_one

# Relationship is defined in the 'orders' dbt semantic model (models/marts/orders.yml)
- join_from_view: dbt_sleon_prod__orders
  join_to_view: dbt_sleon_prod__customers
  join_type: always_left
  on_sql: ${dbt_sleon_prod__orders.customer_id} =
    ${dbt_sleon_prod__customers.customer_id}
  relationship_type: assumed_many_to_one

# Relationship is defined in the 'order_item' dbt semantic model (models/marts/order_items.yml)
- join_from_view: dbt_sleon_prod__order_items
  join_to_view: dbt_sleon_prod__orders
  join_type: always_left
  on_sql: ${dbt_sleon_prod__order_items.order_id} = ${dbt_sleon_prod__orders.order_id}
  relationship_type: assumed_many_to_one

# Relationship is defined in the 'order_item' dbt semantic model (models/marts/order_items.yml)
- join_from_view: dbt_sleon_prod__order_items
  join_to_view: dbt_sleon_prod__products
  join_type: always_left
  on_sql: ${dbt_sleon_prod__order_items.product_id} =
    ${dbt_sleon_prod__products.product_id}
  relationship_type: assumed_many_to_one

- join_from_view: dbt_sleon_prod__stg_customers
  join_to_view: dbt_sleon_prod__customers
  join_type: always_left
  on_sql: ${dbt_sleon_prod__stg_customers.customer_id} =
    ${dbt_sleon_prod__customers.customer_id}
  relationship_type: assumed_many_to_one

- join_from_view: dbt_sleon_prod__stg_locations
  join_to_view: dbt_sleon_prod__locations
  join_type: always_left
  on_sql: ${dbt_sleon_prod__stg_locations.location_id} =
    ${dbt_sleon_prod__locations.location_id}
  relationship_type: assumed_many_to_one

- join_from_view: dbt_sleon_prod__stg_orders
  join_to_view: dbt_sleon_prod__orders
  join_type: always_left
  on_sql: ${dbt_sleon_prod__stg_orders.order_id} = ${dbt_sleon_prod__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: dbt_sleon_prod__stg_orders
  join_to_view: dbt_sleon_prod__locations
  join_type: always_left
  on_sql: ${dbt_sleon_prod__stg_orders.location_id} =
    ${dbt_sleon_prod__locations.location_id}
  relationship_type: assumed_many_to_one

- join_from_view: dbt_sleon_prod__stg_orders
  join_to_view: dbt_sleon_prod__customers
  join_type: always_left
  on_sql: ${dbt_sleon_prod__stg_orders.customer_id} =
    ${dbt_sleon_prod__customers.customer_id}
  relationship_type: assumed_many_to_one

- join_from_view: dbt_sleon_prod__stg_order_items
  join_to_view: dbt_sleon_prod__order_items
  join_type: always_left
  on_sql: ${dbt_sleon_prod__stg_order_items.order_item_id} =
    ${dbt_sleon_prod__order_items.order_item_id}
  relationship_type: assumed_many_to_one

- join_from_view: dbt_sleon_prod__stg_order_items
  join_to_view: dbt_sleon_prod__orders
  join_type: always_left
  on_sql: ${dbt_sleon_prod__stg_order_items.order_id} =
    ${dbt_sleon_prod__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: dbt_sleon_prod__stg_order_items
  join_to_view: dbt_sleon_prod__products
  join_type: always_left
  on_sql: ${dbt_sleon_prod__stg_order_items.product_id} =
    ${dbt_sleon_prod__products.product_id}
  relationship_type: assumed_many_to_one

- join_from_view: dbt_sleon_prod__stg_products
  join_to_view: dbt_sleon_prod__products
  join_type: always_left
  on_sql: ${dbt_sleon_prod__stg_products.product_id} =
    ${dbt_sleon_prod__products.product_id}
  relationship_type: assumed_many_to_one

- join_from_view: dbt_sleon_prod__stg_supplies
  join_to_view: dbt_sleon_prod__products
  join_type: always_left
  on_sql: ${dbt_sleon_prod__stg_supplies.product_id} =
    ${dbt_sleon_prod__products.product_id}
  relationship_type: assumed_many_to_one

- join_from_view: dbt_sleon_prod__supplies
  join_to_view: dbt_sleon_prod__products
  join_type: always_left
  on_sql: ${dbt_sleon_prod__supplies.product_id} =
    ${dbt_sleon_prod__products.product_id}
  relationship_type: assumed_many_to_one

# Relationship is defined in the 'orders' dbt semantic model (models/marts/orders.yml)
- join_from_view: omni_dbt__orders
  join_to_view: omni_dbt__customers
  join_type: always_left
  on_sql: ${omni_dbt__orders.customer_id} = ${omni_dbt__customers.customer_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__stg_customers
  join_to_view: omni_dbt__customers
  join_type: always_left
  on_sql: ${omni_dbt__stg_customers.customer_id} = ${omni_dbt__customers.customer_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__stg_orders
  join_to_view: omni_dbt__customers
  join_type: always_left
  on_sql: ${omni_dbt__stg_orders.customer_id} = ${omni_dbt__customers.customer_id}
  relationship_type: assumed_many_to_one

# Relationship is defined in the 'orders' dbt semantic model (models/marts/orders.yml)
- join_from_view: omni_dbt__orders
  join_to_view: omni_dbt__locations
  join_type: always_left
  on_sql: ${omni_dbt__orders.location_id} = ${omni_dbt__locations.location_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__stg_locations
  join_to_view: omni_dbt__locations
  join_type: always_left
  on_sql: ${omni_dbt__stg_locations.location_id} = ${omni_dbt__locations.location_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__stg_orders
  join_to_view: omni_dbt__locations
  join_type: always_left
  on_sql: ${omni_dbt__stg_orders.location_id} = ${omni_dbt__locations.location_id}
  relationship_type: assumed_many_to_one

# Relationship is defined in the 'order_item' dbt semantic model (models/marts/order_items.yml)
- join_from_view: omni_dbt__order_items
  join_to_view: omni_dbt__orders
  join_type: always_left
  on_sql: ${omni_dbt__order_items.order_id} = ${omni_dbt__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__stg_orders
  join_to_view: omni_dbt__orders
  join_type: always_left
  on_sql: ${omni_dbt__stg_orders.order_id} = ${omni_dbt__orders.order_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__stg_order_items
  join_to_view: omni_dbt__orders
  join_type: always_left
  on_sql: ${omni_dbt__stg_order_items.order_id} = ${omni_dbt__orders.order_id}
  relationship_type: assumed_many_to_one

# Relationship is defined in the 'order_item' dbt semantic model (models/marts/order_items.yml)
- join_from_view: omni_dbt__order_items
  join_to_view: omni_dbt__products
  join_type: always_left
  on_sql: ${omni_dbt__order_items.product_id} = ${omni_dbt__products.product_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__stg_order_items
  join_to_view: omni_dbt__order_items
  join_type: always_left
  on_sql: ${omni_dbt__stg_order_items.order_item_id} =
    ${omni_dbt__order_items.order_item_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__stg_order_items
  join_to_view: omni_dbt__products
  join_type: always_left
  on_sql: ${omni_dbt__stg_order_items.product_id} = ${omni_dbt__products.product_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__stg_products
  join_to_view: omni_dbt__products
  join_type: always_left
  on_sql: ${omni_dbt__stg_products.product_id} = ${omni_dbt__products.product_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__stg_supplies
  join_to_view: omni_dbt__products
  join_type: always_left
  on_sql: ${omni_dbt__stg_supplies.product_id} = ${omni_dbt__products.product_id}
  relationship_type: assumed_many_to_one

- join_from_view: omni_dbt__supplies
  join_to_view: omni_dbt__products
  join_type: always_left
  on_sql: ${omni_dbt__supplies.product_id} = ${omni_dbt__products.product_id}
  relationship_type: assumed_many_to_one
